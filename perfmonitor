#!/bin/bash
# PerfMonitor CLI Wrapper

# Find the directory where this script is located
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# Run the Kotlin script passing all arguments
kotlinc -script "$DIR/PerfMonitor.main.kts" -- "$@"
